//Always compare same data type
console.log( null> 0)
console.log( null ==  0)
console.log( null >= 0)
//this language is inconsistent but we have to live with it. We have to be careful while comparing values of different data types. It is always better to compare values of same data type.
//comparisons convert null to a number, treating is as 0. Thats why null >= 0 is true but null>0 is false

console.log("2" > 1);
console.log("02" > 1);


console.log( undefined ==  0)

//comparison and equality check is different

// === strict check(checks between datatype)

console.log("2" === 2)

